const initState = {
  test: [],
};

const calculator = (state = initState, action) => {
  switch (action.type) {
    case '':
      return state;
    default:
      return state;
  }
};

export default calculator;
